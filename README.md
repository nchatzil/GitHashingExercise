This project contains a short script(hash_script) that takes as input a 4 digit integer and
outputs the hash of that input using SHA-256.You can run it by typing bash hash_script.The 
input is provided during run time.There is also an example of an output using my AEM, stored
in hash_output.txt.

